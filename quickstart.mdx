---
title: 'Bleed Bot Administrator Guide'
description: 'Complete guide to setting up and managing Bleed Bot on your Discord server'
---

## Getting Started

### Invite the Bot to Your Server
1. Use our [official invite link](https://discord.com/oauth2/authorize?client_id=YOUR_BOT_ID&permissions=8&scope=bot%20applications.commands)
2. Select your server from the dropdown
3. Grant the recommended permissions

### Initial Setup
After inviting the bot:
1. Create a "Bleed Bot" role and place it above other roles it needs to manage
2. Use `/setup` to configure default channels and permissions
3. Assign moderator roles with `/modrole set @Role`

## Bot Configuration

<AccordionGroup>
  <Accordion icon="shield-halved" title="Moderation Setup">
    1. Set up your moderation team with `/moderators add @User`
    2. Configure auto-mod with `/automod enable`
    3. Set up logging with `/logchannel #channel`
  </Accordion>

  <Accordion icon="bell" title="Notification Settings">
    - `/welcome enable` - Turn on welcome messages
    - `/welcome message` - Customize the welcome text
    - `/welcome channel #channel` - Set where messages appear
  </Accordion>

  <Accordion icon="sliders" title="Server Preferences">
    - `/prefix set !` - Change command prefix
    - `/language set english` - Set bot language
    - `/timezone set EST` - Configure timezone for timestamps
  </Accordion>
</AccordionGroup>

## Command Categories

### 🛡️ Moderation Tools
| Command | Description | Example |
|---------|-------------|---------|
| `/ban @user [reason]` | Permanently remove a user | `/ban @Spammer Reason: Repeated violations` |
| `/mute @user 1h` | Temporarily mute a user | `/mute @NoisyUser 30m` |
| `/warn @user [reason]` | Issue a formal warning | `/warn @RuleBreaker Reason: Offensive language` |
| `/purge 50` | Clean up messages | `/purge 20` |

### 🛠️ Server Management
| Command | Description | Example |
|---------|-------------|---------|
| `/roleinfo @Role` | Show role details | `/roleinfo @Moderator` |
| `/serverinfo` | Display server statistics | `/serverinfo` |
| `/userinfo @User` | View member information | `/userinfo @NewMember` |

### 🎉 Entertainment
| Command | Description | Example |
|---------|-------------|---------|
| `/meme` | Share a random meme | `/meme` |
| `/joke` | Tell a joke | `/joke` |
| `/poll "Question"` | Create a quick vote | `/poll "Should we add more channels?"` |

## Advanced Features

<CardGroup>
  <Card title="Auto-Moderation" icon="robot">
    Automatic filtering of bad words, spam, and malicious links
  </Card>

  <Card title="Custom Commands" icon="terminal">
    Create server-specific commands with `/customcmd create greet Hello {user}!`
  </Card>

  <Card title="Temporary Actions" icon="clock">
    Set timed mutes, bans, and role assignments
  </Card>

  <Card title="Detailed Logging" icon="file-lines">
    Comprehensive moderation logs and server activity tracking
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Bot isn't responding">
    1. Check if the bot is online in your member list  
    2. Verify you're using the correct prefix (`/` for slash commands)  
    3. Ensure the bot has proper permissions
  </Accordion>

  <Accordion title="Commands not working">
    - Try re-inviting the bot with proper permissions  
    - Use `/sync` to refresh commands  
    - Check our [support server](https://discord.gg/YOUR_INVITE) for outages
  </Accordion>

  <Accordion title="Feature requests">
    Suggest new features in our [feedback channel](https://discord.gg/YOUR_INVITE)  
    or use `/suggest [your idea]`
  </Accordion>
</AccordionGroup>

## Premium Upgrade

Unlock these exclusive features:
- 24/7 priority support
- Advanced analytics dashboard
- Custom branding
- Unlimited custom commands

Upgrade with `/premium info` or visit our [website](https://bleedbot.example/premium)
